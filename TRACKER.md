# SoloOpsGuide Tracker

Read this after `AGENT.md`. This is the live project tracker for current state, open priorities, and the next recommended actions.

## Current status summary

- Site: SoloOpsGuide.
- Main SEO posture: wait and watch Google Search Console after the recent crawl-cleanup and priority-signaling passes; do not broaden scope unless the current signals clearly stall.
- Indexed status currently recorded: the homepage is indexed; some lower-priority support/reference pages and utility cluster pages indexed earlier than desired; `/client-workflow-systems/` has been a key discovered-not-indexed concern.
- Priority cluster posture: the workflow cluster remains the strongest first operational path; blueprint and comparison clusters stay downstream of workflow diagnosis.
- Content structure status: homepage, hubs, cornerstone pages, and support gating have already received multiple tightening passes; support pages should stay subordinate to workflow, blueprint, and comparison pages.
- Strongest current clusters: client workflow systems, software stack blueprints, workflow comparisons.
- Current feature-image status: 42 WebPs active across 6 categories (hubs, blueprints, comparisons, templates, glossary, faq); SVG fallbacks remain only for workflow stage pages; Python/Pillow workflow expanded with 3 new templates (`checklist_card`, `concept_map`, `faq_card`) and 15 total data configs in the templates category; the original 4 approved hero WebPs are untouched; all 15 templates/checklists pages now use topic-specific WebP images; `checklist_card` trigger-label text wrapping fixed — 8 of 15 trigger labels overflowed the 294px trigger box; all 15 images regenerated with fix applied; new `client-closeout-no-response-faq.webp` generated for the new closeout FAQ.
- Current build/deploy status: Cloudflare Pages builds from source, and `public/` is generated output that should remain ignored and untracked.
- Current repo workflow: read `AGENT.md`, read `TRACKER.md`, make a scoped change, update `TRACKER.md`, then commit and push if successful.
- Current operating rhythm: Claude Code is the daily content/improvement tool; Codex is the default weekly review/fix tool and the monthly deeper audit tool; daily Codex use is reserved for heavy build weeks or pre-AdSense submission.
- Repo organization decision still in force: `AGENT.md`, `TRACKER.md`, `README.md`, and `LICENSE` stay at the repo root; non-core process/reference markdown lives under `project-docs/`.

## Active priorities

- Monitor index movement for the workflow hub and anchor before making broader SEO or content changes.
- Keep the workflow cluster as the clear first path from the homepage and hubs.
- Keep support pages intentionally subordinate so they do not outrank cornerstone workflow paths in importance or entry intent.
- Keep the raster-image system clean; future new images should go through the `tools/feature-images/` data + generate workflow, not as ad hoc files.
- Preserve scoped-change discipline: no unrelated refactors, no `public/` commits, and no drift away from the `AGENT.md` plus `TRACKER.md` workflow.

## Current GSC/indexing status

- Most important current watch item: `/client-workflow-systems/` versus lower-priority support/reference pages that have indexed sooner.
- Recorded interpretation after the 2026-04-14 crawl/indexability audit: the site appears technically indexable, and the remaining issue is more likely crawl/index prioritization than a hard blocker such as `noindex`, broken canonicals, or robots blocking.
- Homepage-to-workflow signaling has already been tightened, including stronger workflow-first routing and cleaner priority-page URLs.
- If the workflow anchor indexes before the workflow hub, the hub may still need one more small first-screen authority pass rather than a broad site rewrite.

Priority URLs to monitor:

- `/client-workflow-systems/`
- `/software-stack-blueprints/`
- `/workflow-comparisons/`
- `/workflows/freelance-client-workflow-system-inquiry-to-final-payment/`
- `/guides/software-stack-blueprint-solo-freelancer-lean-budget/`
- `/comparisons/crm-vs-project-management-tool-for-client-workflows/`

## Current image workflow status

- Official image work should stay under `tools/`; do not treat ad hoc asset edits as the primary workflow.
- Current approved production raster path: Python 3.12 + Pillow under `tools/feature-images/`.
- Current raster gate file: `data/feature_image_raster_preferences.json`.
- Approved raster set is intentionally limited to 4 pages:
  - workflow anchor: `freelance-client-workflow-system`
  - workflow hub: `client-workflow-systems`
  - lean blueprint: `solo-freelancer-lean-budget`
  - CRM vs PM comparison: `crm-vs-project-management`
- SVG fallbacks remain for the approved set and for the rest of the feature-image library.
- The local no-API PowerShell workflow also exists under `tools/` for future model-generated image experiments, but it is not required for the current approved WebP set.
- Image visual status: the four approved WebPs were regenerated and refined on 2026-04-17; future polish may still be worthwhile, but only through the approved workflow and only after visual review.

## Current deployment/build policy

- Cloudflare Pages is the deployment model and builds from source with `bash tools/build-cloudflare.sh`.
- `public/` is local/generated build output, not source of truth, and should not be committed in normal passes.
- `.gitignore` explicitly ignores `/public/`.
- Standard verification command when a build is relevant:
  - `tools/hugo/v0.128.0/hugo.exe --gc --minify --baseURL https://soloopsguide.com/`
- For docs-only passes like this tracker cleanup, a Hugo build is not required unless a non-doc source file changes by accident.

## Recent completed passes

- 2026-04-24 template/checklist action layer QA — print layout fix: post-feature review of the action toolbar added earlier; reviewed `layouts/_default/single.html`, `layouts/partials/site-script.html`, and `static/css/main.css`; section gating confirmed correct — `{{ if eq .Section "templates-checklists" }}` correctly restricts the toolbar to template/checklist section pages; re-confirmed rendered output: all 15 template pages have count=2 (`data-article-actions` toolbar + JS selector), while workflow/FAQ/comparison/blueprint/glossary/hub pages have count=1 (JS selector only); copy JS: clones `.article-body`, strips the toolbar node, reads `innerText || textContent`, collapses excess newlines — excludes toolbar labels and header/footer/nav chrome; print CSS: toolbar carries `data-no-print` so it is hidden in print; JS early exit `if (articleActions)` confirmed — non-template pages skip the entire article-actions block cleanly with no JS errors; tracking: copy/print buttons fire `asset_action` through the existing `[data-track]` delegated handler; one real issue found: `.article-header` is `display: grid` (two-column: main content + aside) but was absent from the `@media print` display reset — with `.article-aside` hidden in print, the grid still allocated its second column (min-width 280px), leaving the article title and description spanning only ~60% of the print page width instead of full width; fix: added `.article-header` to the existing print reset rule in `static/css/main.css` (`display: block`), collapsing the grid to block layout for print and allowing the title/description to span full width; fix confirmed in built output (extracted print block from `public/css/main.css`, `.article-header` now present in the reset list); Hugo build clean at 81 pages and 103 static files, no warnings; `public/` remains ignored/untracked; files changed: `static/css/main.css`, `TRACKER.md`.

- 2026-04-24 template/checklist action layer — copy + print: added a lightweight action toolbar that appears only on `templates-checklists` section pages (15 asset pages); chosen as the next feature after discovery/navigation improvements (internal search, guided "Start Here" chooser, Problem-to-Page Guide) because those passes strengthened how readers find the right asset, while template/checklist pages themselves still read like prose rather than operational tools — the highest-value next step was making landed assets feel usable in place; kept deliberately minimal: two actions, one toolbar, no app-like UI; (1) "Copy as plain text" — serializes `innerText` of `.article-body` (excluding the toolbar itself), prefers `navigator.clipboard.writeText` with a `document.execCommand('copy')` fallback via an offscreen textarea for older/insecure contexts, flashes a "Copied" state on the button for 1.8s; (2) "Print or save as PDF" — calls `window.print()` and relies on a small scoped print stylesheet; placement: rendered inside `.article-body.prose` at the top, above the content, so copy output starts from the first heading of the page and print preview opens on the actual asset; scoping: gated in `layouts/_default/single.html` with `{{ if eq .Section "templates-checklists" }}`; JS added inside the existing inline `layouts/partials/site-script.html` (no new JS file, no new HTTP request) — self-contained `[data-article-actions]` block that exits cleanly when the element is absent, so every other page pays essentially no cost; tracking: both buttons fire `asset_action` events through the existing `[data-track]` delegated handler with `data-track-location="template-actions"` and labels `copy`/`print`; UI tokens reuse existing patterns (`#f8fafc` panel background, `#0f766e` teal focus/hover, same 8–12px radii as other cards); print stylesheet hides `.site-header`, `.site-footer`, `.article-aside`, `[data-back-to-top]`, `.related-panel`, `.page-type-block`, `.article-meta`, `.article-hero-media`, `nav`, `form`, and anything tagged `[data-no-print]` (which includes the toolbar itself, so the printed page shows the asset content only), forces `.article-body` links to underlined black, and collapses the two-column reading layout to full-width; chose inline JS over a separate `.js` file because the snippet is small, the repo's existing pattern already inlines all interactive behavior in `site-script.html`, and keeping it there avoids a second script fetch on every page; did not add any "Use this with" section because `related` + best-next routing + the Problem-to-Page Guide already cover that need — adding another inline routing block would have duplicated existing affordances; Hugo build clean at 81 pages and 103 static files, no warnings; QA confirmed rendered output: template pages (`client-onboarding-checklist-for-freelancers-and-consultants`, `weekly-client-operations-checklist-solo-business`, `invoice-and-payment-workflow-checklist-for-service-businesses`, `approval-and-feedback-routing-worksheet-for-multi-stakeholder-review`, all other 11 templates) each contain exactly one `data-article-actions` toolbar plus the shared JS selector (count=2), while workflow/FAQ/comparison/blueprint/glossary/hub pages contain only the JS selector and no toolbar (count=1), confirming the section gate works; `public/` remains ignored/untracked; files changed: `layouts/_default/single.html`, `layouts/partials/site-script.html`, `static/css/main.css`, `TRACKER.md`.

- 2026-04-24 new routing page — Problem-to-Page Guide for Solo Operators: created `content/problem-to-page-guide.md` at `/problem-to-page-guide/` as a standalone decision-aid page that maps named solo-operator symptoms to the strongest single next page; chosen as the next feature after internal search (Phase 1 MVP) and the homepage guided "Start Here" chooser because search helps readers who can name their query, the chooser helps readers who cannot yet name the cluster, and the remaining gap was a practical "I have this exact symptom, where do I go?" asset that routes directly to a specific page rather than a hub; picked a standalone guide (not a new hub) to avoid hub proliferation and to keep the page narrow, editorial, and subordinate to the existing cluster architecture; page structure: intro that routes readers with broader lifecycle problems upstream to the workflow anchor, a "How to use this page" block, five scannable two-column tables (Delivery/approval/review, Billing/payment, Onboarding/intake/kickoff, Closeout/offboarding/recovery, Tool/system-of-record/stack) each with Problem → What it really is → Read first → Next if needed columns, plus a final Definition/term block and a "When this page is the right starting point" section; mappings favor the best exact destination (specific FAQ, workflow, worksheet, or comparison) rather than always routing to a broad hub — e.g., informal "looks good" goes straight to the approval FAQ, system-of-record ambiguity goes straight to the CRM vs PM comparison, billing-location drift goes straight to the billing-status comparison, scope recovery goes to the scope-reset worksheet; feature image created through the approved Python/Pillow workflow: new data config `tools/feature-images/feature_images/data/hubs/problem-to-page-guide.json` using `hub_overview` layout with `workflow` palette, generating `static/images/features/hubs/problem-to-page-guide.webp` — anchor "Name the blocker first" with four cluster modules (Workflow / Comparison / Blueprint / Support); front matter has valid `feature_image`, `feature_image_alt`, canonical URL, and JSON-LD; one small contextual backlink added from the homepage chooser section in `layouts/index.html` — a single `homepage-inline-link` under the chooser intro reading "See the full problem-to-page matrix" with `data-track-label=problem-to-page-guide`; chose only that one backlink because the homepage chooser is the strongest upstream for this routing page and broader backlink sprinkling would turn narrow support pages into navigation stubs; Hugo build clean at 81 pages (up from 80) and 103 static files (up from 102 — new WebP), no warnings; QA confirmed rendered `/problem-to-page-guide/index.html` contains the correct title, canonical, feature image meta/OG/Twitter, Article + BreadcrumbList JSON-LD, and the full table content (spot-checked `/comparisons/best-home-for-billing-status-invoicing-tool-vs-system-of-record/`, `/templates/approval-and-feedback-routing-worksheet-for-multi-stakeholder-review/`, `/templates/escalation-and-pause-state-worksheet-for-solo-operators/`, `/templates/client-offboarding--testimonial-request-template/`, `/workflows/how-to-build-a-client-intake-and-qualification-workflow/`, `/guides/how-to-migrate-from-scattered-tools-to-one-workflow-system/`, `/faq/what-to-do-when-required-client-inputs-are-late-or-incomplete/` — all resolve), homepage backlink renders in `public/index.html`, and `public/` remains ignored/untracked; files changed: `content/problem-to-page-guide.md` (new), `tools/feature-images/feature_images/data/hubs/problem-to-page-guide.json` (new), `static/images/features/hubs/problem-to-page-guide.webp` (new), `layouts/index.html` (one inline backlink), `TRACKER.md`.

- 2026-04-24 weekly Codex audit - homepage and hub first-click clarity: ran a scoped review of the homepage and major hub entry layers in audit/review mode; read `AGENT.md`, `TRACKER.md`, and `project-docs/INTERNAL_SEARCH_ROADMAP.md`; `project-docs/DAY_WORK_PLAN_FROM_NOW.md` remains missing in this checkout; reviewed `content/_index.md`, `layouts/index.html`, `content/client-workflow-systems/_index.md`, `content/software-stack-blueprints/_index.md`, `content/workflow-comparisons/_index.md`, plus homepage card/header/related partial context; homepage hero, problem chooser, resource grid, Start Here section, and footer CTA still make workflow-first routing the clearest broad path, and the workflow/comparison hubs remain appropriately positioned; highest-value issue found was one small blueprint-hub first-click gap: the opening guidance sent readers with an unclear system center to the broad comparison hub, even though the site already treats `CRM vs Project Management Tool for Client Workflows` as the strongest direct decision page for that case; fix: changed that sentence in `content/software-stack-blueprints/_index.md` to route directly to the CRM-vs-PM comparison before treating any blueprint as final; no URL, template, homepage, or structural changes; Hugo build clean with `tools/hugo/v0.128.0/hugo.exe --gc --minify --baseURL https://soloopsguide.com/` at 80 pages and 102 static files; QA confirmed rendered `/software-stack-blueprints/` contains the direct CRM-vs-PM link in the first-screen hub copy and `public/` remains ignored/untracked; files changed: `content/software-stack-blueprints/_index.md`, `TRACKER.md`.

- 2026-04-24 weekly Codex audit - support-layer hierarchy heading correction: ran a scoped review pass focused on whether templates/checklists and FAQ pages still stay subordinate to the workflow-first cluster; read `AGENT.md`, `TRACKER.md`, and `project-docs/INTERNAL_SEARCH_ROADMAP.md`; `project-docs/DAY_WORK_PLAN_FROM_NOW.md` is still not present in this checkout; reviewed `content/templates-checklists/_index.md`, `content/faq/_index.md`, `content/client-workflow-systems/_index.md`, `content/client-workflow-systems/sample-client-workflow.md`, representative template/checklist pages (`sample-onboarding-checklist.md`, `weekly-client-operations-checklist.md`, `invoice-payment-workflow-checklist.md`, `approval-and-feedback-routing-worksheet.md`, `client-offboarding-testimonial-template.md`), and representative FAQ pages including the newer approval/closeout FAQs; workflow hub and anchor still route strongly into the main operational path, and FAQ/newer closeout/approval pages remain narrow enough; highest-value issue found was a small but confusing heading pattern on older support pages: `## Start here first if...` introduced upstream-routing sections that actually tell readers to leave the support page and start with a stronger workflow/blueprint/comparison page; fix: changed that heading to `## Start upstream first if...` on seven existing support pages, leaving page bodies, links, URLs, and templates unchanged; Hugo build clean with `tools/hugo/v0.128.0/hugo.exe --gc --minify --baseURL https://soloopsguide.com/` at 80 pages and 102 static files; QA confirmed no remaining `Start here first if` matches in `content/templates-checklists`, `content/faq`, `public/templates`, or `public/faq`, confirmed rendered pages contain `Start upstream first if`, and `public/` remains ignored/untracked; files changed: seven support markdown pages plus `TRACKER.md`.

- 2026-04-24 weekly Codex audit - internal search natural-query correction: ran the requested scoped review pass in audit/review mode, starting with `AGENT.md`, `TRACKER.md`, and `project-docs/INTERNAL_SEARCH_ROADMAP.md`; `project-docs/DAY_WORK_PLAN_FROM_NOW.md` was requested but is not present in this checkout, so the pass continued from the durable repo guidance and live tracker; reviewed current homepage routing (`layouts/index.html`), workflow hub (`content/client-workflow-systems/_index.md`), workflow anchor (`content/client-workflow-systems/sample-client-workflow.md`), lean stack blueprint (`content/software-stack-blueprints/sample-stack-blueprint.md` - the requested `sample-solo-freelancer-lean-budget.md` path is not present), CRM vs PM comparison (`content/workflow-comparisons/sample-crm-vs-pm.md`), FAQ hub (`content/faq/_index.md`), milestone delivery, invoice/payment, offboarding, the newer approval and closeout FAQ pages, and the internal search template/index/header/script; workflow-first hierarchy, approval/billing/closeout links, FAQ subordination, and feature-image references were acceptable, and no routing/content/image fix was justified; highest-value issue found was search live usability: exact short searches worked, but natural operational queries could return zero or weak results because every token was required and common filler/response wording was not normalized (examples before fix: "client won't respond", "where to bill after approval", "final closeout no reply", "payment overdue"); one surgical fix applied in `static/js/search.js`: added a small stop-word filter for question/filler tokens and targeted synonyms for `respond`/`response`/`responding`/`reply`/`unanswered` plus `late`/`overdue`, keeping the existing lightweight Phase 1 matcher and URL/filter behavior unchanged; no URL, content, routing, template, or image changes; Hugo build clean with `tools/hugo/v0.128.0/hugo.exe --gc --minify --baseURL https://soloopsguide.com/` at 80 pages and 102 static files; QA confirmed rendered `/search/` keeps `noindex,follow`, rendered config remains `SoloOpsGuideSearch={indexUrl:"/search-index.json"}`, generated `public/js/search.js` contains the new normalization, representative searches now route to relevant existing pages (`client won't respond` -> closeout/silence FAQs, `final closeout no reply` -> closeout FAQ, `where to bill after approval` -> approval-before-billing FAQ, `payment overdue` -> invoice/payment assets), no missing feature-image files were found, and `public/` remains ignored/untracked; files changed: `static/js/search.js`, `TRACKER.md`.

- 2026-04-24 internal search bugfix — "temporarily unavailable" on production: root cause was Hugo's inline-JS minifier mangling the `window.SoloOpsGuideSearch` config in `layouts/_default/search.html`; the template used `{{ "search-index.json" | relURL | jsonify }}` which Hugo renders as `"/search-index.json"` (a JSON-quoted string), but the `--minify` pass then wrapped the entire quoted value in an additional pair of single quotes, so the rendered `public/search/index.html` contained `SoloOpsGuideSearch={indexUrl:'"/search-index.json"'}` — a 22-char string starting with a literal double quote instead of a URL path; `fetch(indexUrl)` therefore rejected and the catch branch rendered "Search is temporarily unavailable. Please try again in a moment."; the `/search-index.json` file itself was generated correctly (57.7K, 62 pages, valid JSON, no BOM) — this was purely a template-side quoting/minifier bug, not an index generation bug; fix: replaced `{{ "search-index.json" | relURL | jsonify }}` with `"{{ "search-index.json" | relURL }}"` (manual quoting so `jsonify` is no longer in the pipeline and the minifier cannot double-wrap); chose this over removing the inline config entirely because the `| relURL` call preserves baseURL flexibility even though the site is always served at root; rebuilt with `tools/hugo/v0.128.0/hugo.exe --gc --minify --baseURL https://soloopsguide.com/` and confirmed rendered output is now `SoloOpsGuideSearch={indexUrl:"/search-index.json"}` (correct); search.js `config.indexUrl` now resolves to `/search-index.json` so `fetch()` succeeds and scored results render; representative queries verified locally against the index shape (approval, billing, onboarding, handoff, closeout, client silence, CRM, PM — all resolvable against existing scoring + synonym map from the Phase 1 MVP); Hugo build clean at 80 pages, 102 static files; `public/` remains ignored and untracked; files changed: `layouts/_default/search.html`, `TRACKER.md`.

- 2026-04-24 homepage guided "Start Here" decision helper: added a compact problem-first chooser to the homepage between the hero and the value props section — a new `homepage-chooser` section with six situation-to-destination cards, each routing to the best single page rather than just a broad category hub; the six paths are (1) "My client process feels loose or messy" → workflow anchor `/workflows/freelance-client-workflow-system-inquiry-to-final-payment/`, (2) "I know the stage, but a tool or system choice is blocking me" → CRM vs PM comparison `/comparisons/crm-vs-project-management-tool-for-client-workflows/` (chosen over the comparisons hub because it is the strongest single comparison page), (3) "The workflow is clear but my stack feels heavy or awkward" → lean stack blueprint `/guides/software-stack-blueprint-solo-freelancer-lean-budget/` (chosen over the blueprints hub because the lean blueprint is the default entry page), (4) "I need an execution asset for a specific trigger moment" → templates hub `/templates-checklists/`, (5) "I have a narrow blocker (approval, billing, silence, closeout)" → FAQ hub `/faq/`, (6) "One term is unclear and that is the only blocker" → glossary `/glossary/`; placement chosen between hero and value-props so a confused first-time visitor sees situation-first routing before the value narrative and the existing type-first "Explore by resource type" grid; kept intentionally simple as plain anchor cards with no JS — each card has a bold situation line and a subtler teal destination line, 3x2 grid on desktop, 2x1 on tablet, 1-col on mobile; tracking events `cta_click` with `data-track-location="homepage-chooser"` and per-situation labels (`situation-messy-process`, `situation-tool-choice`, `situation-heavy-stack`, `situation-asset`, `situation-narrow-blocker`, `situation-term`); CSS appended to `static/css/main.css` reuses existing tokens (same border color, radius system, teal accent, box-shadow as other homepage cards) and the chooser grid was added to both the 1080px and 820px responsive breakpoints so it collapses cleanly; the existing "Start Here" priority-pages section lower on the page was left untouched — it showcases four specific cornerstone pages and is complementary to the problem-first chooser, not duplicative; chosen as the next feature after internal search because search helps readers who can name their problem, while the chooser helps readers who cannot yet name it and need a situation-first entry point; Hugo build clean at 80 pages, 102 static files (no new pages or static files — purely template + CSS); QA confirmed all six cards rendered in `public/index.html` with correct hrefs, labels, and tracking attributes; `public/` remains ignored and untracked; files changed: `layouts/index.html`, `static/css/main.css`, `TRACKER.md`.

- 2026-04-23 internal search implementation + roadmap: replaced the header DuckDuckGo `site:` search with a real on-site internal search backed by a Hugo-generated JSON index; chosen because the site's workflow-first cluster model cannot be expressed through external search ranking and readers needed problem-first discovery (approval, billing, onboarding, handoff, closeout, client silence) that goes beyond exact title matching; architecture: (1) Hugo custom output format `SearchIndex` on the home kind emits `/search-index.json` at build time listing every non-draft regular page and section hub with url/title/section/kind/type/role/description/summary/tags/category plus a 360-char body excerpt (62 indexed pages), (2) new `/search/` page via `content/search.md` with dedicated `layouts/_default/search.html` rendering an input, section filter chips (Workflows, Blueprints, Comparisons, Templates, Glossary, FAQ, All), and results list, (3) vanilla `static/js/search.js` client-side matcher with weighted scoring (title x10, tags/type/category x6, description/summary x4, body x1), inline synonym map for problem-first queries, require-all-tokens gating, URL-state sync via `?q=` and `?type=`, and `internal_search_query` tracking event; header form `layouts/partials/header.html` now posts to `/search/` with `data-internal-search` and the old DuckDuckGo `site:` prefix handler in `layouts/partials/site-script.html` was stripped to a plain tracking submit; `layouts/partials/head.html` gained a `Params.robots` override so `content/search.md` can set `noindex,follow`; small CSS appended to `static/css/main.css` for search page form, filter chips, status, and result cards; chose vanilla JS over Fuse/Lunr/Pagefind because the catalog is small enough that substring + synonym matching is sharper and avoids adding a dependency; spot-check scoring across the built index: "approval" → approval FAQ + approval owner glossary + approval routing worksheet; "billing" → invoice/payment workflow + billing system-of-record comparison + invoice/payment checklist; "onboarding" → client onboarding workflow + onboarding checklist; "CRM" → CRM vs PM comparison; "closeout" → closeout non-response FAQ + offboarding workflow + offboarding template; "client silence" → silence FAQ + closeout FAQ; "handoff" → delivery QA checklist + milestone delivery workflow + proposal-to-contract handoff; "workflow" → comparisons hub + proposal revision workflow + change request workflow; roadmap document `project-docs/INTERNAL_SEARCH_ROADMAP.md` created with Phase 1 (MVP — shipped), Phase 2 (keyword chips, external synonym data file, stop-words/plurals, empty-state priority pages, per-section ranking rules, header suggestions, auto-widen no-results), Phase 3 (problem-to-page mapping, guided cluster framing, intent classification, stage-page routing handoff, query-log review cadence, structured result cards); Hugo build clean at 80 pages (up from 78 — added `/search/` and `/search-index.json`) with 102 static files (up from 101 — added `js/search.js`); QA confirmed rendered search page contains `data-search-form`, `data-search-filters`, `data-search-results`, `data-search-status`, correct robots `noindex,follow` meta, and script references to `search-index.json` and `js/search.js`; header form in `public/index.html` confirmed as `action=/search/ method=get data-internal-search=true`; `public/` remains ignored and untracked.

- 2026-04-23 weekly cluster review - approval/closeout support chain: reviewed `content/client-workflow-systems/sample-client-workflow.md`, `content/client-workflow-systems/milestone-delivery-workflow-for-solo-service-businesses.md`, `content/client-workflow-systems/invoice-and-payment-workflow-setup-for-freelancers-and-consultants.md`, `content/client-workflow-systems/client-offboarding-workflow-for-freelancers-and-solo-service-businesses.md`, `content/faq/what-counts-as-client-approval-before-billing-or-next-stage.md`, `content/faq/what-to-do-when-client-does-not-respond-to-project-closeout.md`, and `content/faq/_index.md`; the workflow anchor, milestone delivery page, invoice/payment page, and FAQ hub already had the intended approval/closeout support routing, so the only content change was one wording/link cleanup in `content/client-workflow-systems/client-offboarding-workflow-for-freelancers-and-solo-service-businesses.md` - replaced the stale review-silence follow-on in the completion standard with the closeout-specific FAQ route: "If final approval and billing are already settled but the client does not acknowledge the formal project end, continue to [FAQ: What Should I Do When a Client Does Not Respond to Final Project Closeout?]"; chosen because it fixed the weakest remaining support handoff in the chain without broadening into a multi-page audit or adding repetitive support-page mentions; Hugo build clean at 78 pages, 101 static files; QA confirmed the new offboarding rendered link in `public/workflows/client-offboarding-workflow-for-freelancers-and-solo-service-businesses/index.html` and both newer FAQ links still render in `public/faq/index.html`; `public/` remains ignored and untracked.

- 2026-04-23 FAQ hub support-layer integration: added the two newer approval/closeout FAQ pages to `content/faq/_index.md` across three places — (1) "Fastest useful starting points": added two descriptive entries — "Open [FAQ: What Counts as Client Approval...] when a client response has arrived but is not clearly a formal approval" and "Open [FAQ: What Should I Do When a Client Does Not Respond to Final Project Closeout?] when all deliverables are accepted and billing is settled, but the client has not acknowledged the formal project end"; also tightened the existing client-silence entry from "when approval or feedback is stalled" to "when approval or feedback is stalled mid-project" to better distinguish it from the closeout scenario; (2) "Start with the blocker, not the whole library": added the same two FAQ pages as plain links to match the existing pattern; (3) "Questions this section is built to answer": added "approval clarity before billing or the next stage starts" and "final project closeout non-response after all work is accepted"; chosen because both new FAQs were reachable from deep workflow pages but not signaled from the FAQ hub itself — readers arriving at the hub from search would not see them without browsing the full card grid; the improvement keeps the hub useful as a routing layer without adding new sections or weakening workflow-first posture; Hugo build clean at 78 pages, 101 static files; QA confirmed both new FAQ links appear in rendered hub in both the descriptive list and the plain-link list, and both pages' cards render with correct WebP images; `public/` remains ignored and untracked.

- 2026-04-23 broken feature image fix — client approval signoff FAQ: root cause was `content/faq/what-counts-as-client-approval-before-billing-or-next-stage.md` front matter referencing `/images/features/faq/client-approval-signoff-faq.svg` — a file that never existed in `static/images/features/faq/` (no SVG and no WebP); the page was created in Day 5 with an SVG placeholder path and the image asset was explicitly deferred; fix: created data config `tools/feature-images/feature_images/data/faq/client-approval-signoff-faq.json` using `faq_card` layout, `workflow` palette; image content: eyebrow "FAQ", title "What Counts as Approval?", subtitle "How to confirm a client response actually closed the milestone before billing or the next stage starts", problem box "Informal response only / Response received but milestone close not explicitly confirmed", four resolution routes ("Check named approval owner", "Confirm channel and format", "Log the decision explicitly", "Hold billing until confirmed"); generated `static/images/features/faq/client-approval-signoff-faq.webp` with no warnings; updated front matter from `.svg` to `.webp`; Hugo build clean at 78 pages, 101 static files (up from 100); QA confirmed only `.webp` references in `public/faq/what-counts-as-client-approval-before-billing-or-next-stage/index.html`; `public/` remains ignored and untracked.

- 2026-04-23 workflow anchor integration of closeout non-response FAQ: added one contextual link to `content/client-workflow-systems/sample-client-workflow.md` — "Closeout non-response follow-on: FAQ: What Should I Do When a Client Does Not Respond to Final Project Closeout?" appended after the Stage 6 (Offboarding and expansion) implementation asset, using the existing follow-on link pattern established in Stages 3, 4, and 5; chosen placement because Stage 6 is where a project formally ends, and the offboarding section already correctly describes the output as "testimonial request, closeout doc, and next-step opportunity" but had no guidance for the specific blocker where the formal close is not acknowledged by the client; this is the lifecycle-level link that carries the new FAQ from cluster support (offboarding workflow already links to it from Day 11) into the anchor page where readers diagnose end-of-lifecycle blockers before choosing the exact stage page; no other links changed, no new sections added; Hugo build clean at 78 pages, 100 static files; QA confirmed "Closeout non-response follow-on" and link to `/faq/what-to-do-when-client-does-not-respond-to-project-closeout/` rendered correctly in `public/workflows/freelance-client-workflow-system-inquiry-to-final-payment/index.html`; `public/` remains ignored and untracked.

- 2026-04-22 new FAQ: what to do when client does not respond to final project closeout: created `content/faq/what-to-do-when-client-does-not-respond-to-project-closeout.md` at `/faq/what-to-do-when-client-does-not-respond-to-project-closeout/`; chosen as an FAQ (not a template) because the blocker is operational — what to do next — not an execution asset; the "not approved or rejected" case is already fully covered by the existing `what-to-do-when-client-has-not-approved-or-rejected-deliverables.md` (created Day 9), so this page fills the distinct remaining gap: the FINAL project closeout scenario where all deliverables were accepted and billing is resolved, but the client has not acknowledged the formal end of the engagement, blocking testimonial request, project archiving, and capacity release; clear boundary from existing pages: client-silence FAQ covers mid-project review silence, pending-approval FAQ covers unclear milestone decisions, this page covers post-completion engagement close non-response; key sections: what counts as project closeout operationally, when to send the closeout message, what the message should say (example structure included), how long to wait, what the second message should say, when functional close applies (after 2 attempts with documentation), what must not happen without confirmation, where to go next; internal links: upstream to client offboarding workflow (primary) + milestone delivery workflow, lateral to client-silence FAQ + escalation worksheet, downstream to client offboarding template; feature image: `client-closeout-no-response-faq.webp` using `faq_card` layout, `workflow` palette; problem box: "No closeout confirmation / Deliverables accepted, billing resolved, but engagement close not acknowledged"; 4 resolution routes: "Send structured closeout message", "Wait the set review window", "Follow up once more", "Apply functional close"; Hugo build clean at 78 pages (up from 77) with 100 static files (up from 99); QA confirmed correct `.webp` OG and Twitter image references and 5 internal link references in rendered output; `public/` remains ignored and untracked.

- 2026-04-22 checklist_card text-wrap fix: fixed trigger-label text overflow in `tools/feature-images/feature_images/templates/checklist_card.py`; root cause was two `draw.text` calls with no width constraint — the trigger label (`draw.text` at y=498) had only 294px horizontal space inside the trigger box, but 8 of 15 labels exceeded that width (measured: 322–367px); step labels (`draw.text` at y+18) had 710px but also used unconstrained drawing; fix: replaced both `draw.text` calls with `draw_text_block` (existing wrapping helper); trigger label now uses `max_lines=2`, `width=294`, start moved to `y=490`; trigger note moved down to `y=578` (from y=554) to accommodate 2-line label; step label uses `draw_text_block` with `width=680`, `max_lines=1` as a width safety guard; regenerated all 15 template WebPs; Hugo build clean at 77 pages, 99 static files; QA confirmed correct `.webp` OG and Twitter image references for `approval-feedback-routing-worksheet` and `escalation-pause-state-worksheet`; `public/` remains ignored and untracked.

- 2026-04-22 templates/checklists feature image pass: generated 11 new topic-specific WebP images to replace generic SVG placeholders across all remaining templates/checklists pages; root cause was a shared SVG template that produced the same composition for every page (same layout: left bar column + right dark panel with 4 boxes), with only text labels varying — not sufficiently topic-specific; fixed by creating 11 JSON data configs in `tools/feature-images/feature_images/data/templates/` using the `checklist_card` layout, each with distinct eyebrow, title, subtitle, trigger, and 4 step rows that match the actual page topic; palettes: `workflow` for 10 client-workflow templates, `blueprint` for system-of-record-rules (stack ownership topic); generated WebPs: `approval-feedback-routing-worksheet`, `client-change-request-template`, `client-input-dependency-worksheet`, `client-offboarding-template`, `delivery-qa-checklist`, `escalation-pause-state-worksheet`, `project-start-readiness-handoff-boundary-worksheet`, `recovery-update-revised-plan-notice-template`, `scope-reset-recovery-worksheet`, `system-of-record-rules-worksheet`, `weekly-client-status-update-template`; updated front matter on all 11 content files from `.svg` to `.webp`; the 4 previously completed template WebPs (client-onboarding-checklist, invoice-payment-checklist, stack-audit-consolidation-worksheet, weekly-client-operations) were not touched; Hugo build clean at 77 pages with 99 static files (up from 88 — 11 new WebPs); QA confirmed correct `.webp` OG and Twitter image references in rendered output for `delivery-qa-checklist-before-client-handoff` and `escalation-and-pause-state-worksheet-for-solo-operators`; `public/` remains ignored and untracked.

- 2026-04-22 feature image for pending-approval-decision FAQ: created `tools/feature-images/feature_images/data/faq/pending-approval-decision-faq.json` using the `faq_card` layout and `comparison` palette; ran `generate_one.py --id pending-approval-decision-faq` to produce `static/images/features/faq/pending-approval-decision-faq.webp`; updated front matter on `content/faq/what-to-do-when-client-has-not-approved-or-rejected-deliverables.md` to reference `.webp` instead of the placeholder `.svg`; image content: eyebrow "FAQ", title "Pending Approval FAQ", subtitle about the decision sequence when stage is still open, problem box "No clear decision received / Stage confirmed open after vague or missing client response", four resolution routes ("Mark state pending", "Follow up with context", "Do not advance stage", "Escalate if blocked"); Hugo build clean at 77 pages with 88 static files (up from 87 — new webp); QA confirmed `pending-approval-decision-faq.webp` rendered correctly in `<img>` tag and OG/Twitter meta in `public/faq/what-to-do-when-client-has-not-approved-or-rejected-deliverables/index.html`; `public/` remains ignored and untracked.

- 2026-04-22 new FAQ: what to do when client has not clearly approved or rejected deliverables: created `content/faq/what-to-do-when-client-has-not-approved-or-rejected-deliverables.md` at `/faq/what-to-do-when-client-has-not-approved-or-rejected-deliverables/`; chosen as an FAQ (not a template or checklist) because the blocker is an operational decision — what to do next — not an execution asset or definitional gap; gap is real and distinct from the two adjacent FAQs: the silence FAQ covers complete non-response, the approval FAQ covers whether a received response counts as approval, and this new page covers the operational sequence after you know the stage is still open — when to follow up, what to say, what must not happen while waiting, and when to escalate to a formal block; key sections: "When must the stage stay open?" (lists four conditions including partial response, call feedback with no follow-up, wrong stakeholder responding), "What should you do while waiting?" (three steps: mark state, record the outstanding decision, set a follow-up date), "When should you follow up?" (operational message structure), "When should you follow up a second time?" (escalation boundary at two follow-ups), "What must not happen while the decision is pending?" (no invoice, no next stage, no assumed acceptance), "When should this become a formal block?"; internal links: upstream to milestone delivery workflow (cornerstone), lateral to approval FAQ + silence FAQ + escalation worksheet, downstream to invoice/payment workflow; Hugo build clean at 77 pages (up from 76) with 87 static files; QA confirmed all links rendered correctly in public output; `public/` remains ignored and untracked; chosen as today's daily pass because the approval-chain cluster (Days 5–9) established clearer criteria for what counts as approval but left a gap in the operational follow-up sequence — what a reader should actually do when the stage is confirmed still open.

- 2026-04-22 offboarding workflow closeout-trigger tightening: one surgical edit to `content/client-workflow-systems/client-offboarding-workflow-for-freelancers-and-solo-service-businesses.md` — added one paragraph at the end of Step 2 (Capture final signoff intentionally), after the existing "Next Action Owner" sentence: "If a response has arrived but feels informal or ambiguous — positive but not explicitly accepting — use [FAQ: What Counts as Client Approval Before Billing or the Next Stage Starts?] to confirm whether the signoff is actually complete before moving into billing closure or archiving."; this is the natural closeout-stage placement for the approval FAQ — Step 2 already correctly lists vague signals that do not count (looks good for now, silence, internal feeling) and states what does count (explicit acceptance, agreed close date), but had no guidance for the in-between case where a response arrived but was ambiguous; the new paragraph fills that exact gap at the moment when a reader is deciding whether to proceed to billing and archive; the invoice/payment workflow was not added inline because it is already linked twice on the page (lifecycle section + "Use this workflow with"); no new sections added, no other paragraphs changed; Hugo build clean at 76 pages with 87 static files; QA confirmed the new paragraph and FAQ link rendered correctly in `public/workflows/client-offboarding-workflow-for-freelancers-and-solo-service-businesses/index.html`; `public/` remains ignored and untracked; chosen as today's daily pass because it carries the approval-chain logic established in Days 5–8 into the final lifecycle stage — delivery → approval → billing → offboarding now all explicitly handle the "received a response but is it actually an approval?" scenario.

- 2026-04-22 invoice/payment workflow billing-trigger tightening: one surgical edit to `content/client-workflow-systems/invoice-and-payment-workflow-setup-for-freelancers-and-consultants.md` — replaced the informal-approval paragraph in Step 3 (Align invoice timing with milestone reality); the old paragraph said "a quick 'looks good' in chat can still trigger billing, but only if you log that acceptance" — this was factually inconsistent with the approval-close logic established in Days 5–7 and would undermine the FAQ and milestone delivery improvements already made; the new paragraph corrects this: "A 'looks good' in chat is not a billing trigger — it is an acknowledgment, not an approval. Billing should not start until the named approval owner gives an explicit decision through the agreed channel and it is logged in the project record." followed by a link to [FAQ: What Counts as Client Approval Before Billing or the Next Stage Starts?](/faq/what-counts-as-client-approval-before-billing-or-next-stage/); the milestone delivery workflow link already present in Step 3 was preserved; no new sections added, no other paragraphs changed; Hugo build clean at 76 pages with 87 static files; QA confirmed the corrected paragraph and FAQ link rendered correctly in `public/workflows/invoice-and-payment-workflow-setup-for-freelancers-and-consultants/index.html`; `public/` remains ignored and untracked; chosen as today's daily pass because Step 3 contained the most important factual correction needed after the approval-close logic work — contradictory guidance on the billing page would have undermined the FAQ and milestone delivery improvements from earlier in the week.

- 2026-04-22 milestone delivery workflow approval-close tightening: two surgical edits to `content/client-workflow-systems/milestone-delivery-workflow-for-solo-service-businesses.md` — (1) added one paragraph at the end of Step 4 (Run QA before client review), after the "approval request must be explicit" paragraph: "If the client responds with positive language but no explicit decision, the review is still open. Use [FAQ: What Counts as Client Approval Before Billing or the Next Stage Starts?](/faq/what-counts-as-client-approval-before-billing-or-next-stage/) to confirm whether a response actually closed the milestone or only acknowledged the work." — this links the new approval FAQ at the exact delivery moment where vague responses cause the most downstream damage; (2) inserted one sentence into the Step 5 (Close the milestone) paragraph, between "Do not leave it in a fuzzy mostly done state" and "That ambiguity is where delivery drift begins": "Positive client feedback and formal approval are not the same milestone outcome — the stage is only closed when the named [approval owner](/glossary/approval-owner/) gives an explicit decision through the agreed channel." — this makes the review/approval distinction explicit at the close-out moment and adds the approval-owner glossary link where the concept is directly relevant; no new sections added, no existing links removed, no structural changes; Hugo build clean at 76 pages with 87 static files; QA confirmed both edits rendered correctly in `public/workflows/milestone-delivery-workflow-for-solo-service-businesses/index.html` with correct link targets; `public/` remains ignored and untracked; chosen as today's daily pass because the milestone delivery page was the natural next cluster-coherence move after creating the approval FAQ (Day 5) and integrating it into the workflow anchor (Day 6) — the delivery stage is where the "positive response but no explicit approval" blocker most naturally first appears, and the page had no language distinguishing the two scenarios.

- 2026-04-22 workflow anchor integration of approval FAQ: added two contextual follow-on links in `content/client-workflow-systems/sample-client-workflow.md` to the new FAQ created on Day 5 — (1) "Approval-check follow-on" added at the end of the Stage 4 (Delivery and QA) link block, after the existing change-request follow-on, because delivery is where approval ambiguity first blocks progress; (2) "Approval-ambiguity follow-on" added at the end of the Stage 5 (Invoicing and payment) link block, because the billing trigger depends on milestone completion being formally approved; both links use different anchor prefixes ("Approval-check" vs "Approval-ambiguity") to reflect distinct entry points — delivery side vs billing-trigger side — and match the existing "follow-on" label pattern used in other stage sections; no new sections added, no existing links changed, no link stuffing; Hugo build clean at 76 pages with 87 static files; QA confirmed both links rendered at correct positions in `public/workflows/freelance-client-workflow-system-inquiry-to-final-payment/index.html`; `public/` remains ignored and untracked; chosen as today's daily pass because the new FAQ was created but had no upstream links from the strongest workflow pages — a new page with no upstream path from the anchor was not yet integrated into the cluster.

- 2026-04-22 new FAQ: what counts as client approval before billing or next stage: created `content/faq/what-counts-as-client-approval-before-billing-or-next-stage.md` at `/faq/what-counts-as-client-approval-before-billing-or-next-stage/`; the page is an FAQ (not a glossary or template) because the blocker is operational rather than definitional — the reader already understands approval in principle but is unsure whether a specific client response actually closed the stage; this is clearly distinct from the three closest existing pages: `approval-owner.md` covers *who* can approve, `approval-and-feedback-routing-worksheet.md` covers *how multi-stakeholder feedback flows*, and `client-goes-silent-during-review.md` covers *what to do when there is no response at all*; the new FAQ covers *what constitutes a valid approval and when billing or the next stage can safely start*; internal links added: upstream to milestone delivery workflow, lateral to approval-owner glossary + approval-routing-worksheet + client-silence FAQ, downstream to invoice/payment workflow and client offboarding; feature image referenced at `/images/features/faq/client-approval-signoff-faq.svg` — SVG will need to be generated through the approved `tools/feature-images/` workflow when image work resumes; Hugo build clean at 76 pages (up from 75) with 87 static files; all internal links confirmed in rendered output; `public/` remains ignored and untracked; chosen as today's daily pass because the gap between informal positive feedback and formal approval is a real recurring blocker that no existing page addresses directly — it sits clearly in the workflow cluster, stays narrow and subordinate to the milestone delivery workflow, and gives the site one more useful, linkable support asset.

- 2026-04-22 CRM vs PM comparison intro tightening: three surgical changes to `content/workflow-comparisons/sample-crm-vs-pm.md` — (1) collapsed 5-paragraph intro into 4: removed the redundant paragraph 4 (which repeated paragraph 3 almost exactly) and rewrote paragraph 3 to remove internal SEO language ("one of the highest-leverage decisions on the site", "the strongest first page") prohibited by AGENT.md, replacing it with a plain statement of why the decision matters operationally; (2) renamed section heading "If you landed here directly" to "When this page is the right starting point", matching the established pattern from the workflow anchor and lean blueprint passes; (3) fixed the "Start somewhere else first if..." routing sentence, which had routed readers to Notion vs ClickUp as a "start somewhere else" option — that is a narrower comparison, not a broader starting point; replaced with clear routing: workflow anchor if lifecycle is still unclear, lean blueprint if system center is already settled and stack shape is the remaining question; Hugo build clean at 75 pages with 87 static files; QA confirmed all three changes present in `public/comparisons/crm-vs-project-management-tool-for-client-workflows/index.html`; `public/` remains ignored and untracked; chosen as today's daily pass because the comparison page intro had the most actionable fixable weaknesses — internal SEO language, a redundant paragraph, and a routing signal that pointed readers to a narrower comparison instead of back to the appropriate broader page.

- 2026-04-22 lean blueprint intro tightening: condensed the 6 opening paragraphs of `content/software-stack-blueprints/sample-stack-blueprint.md` into 3 sharper ones — the "who" (solo operators who want a stack they can actually maintain alone) moved from paragraph 4 to paragraph 1; the operating definition of "lean budget" (minimum paid tool categories chosen only after a real workflow bottleneck makes the need visible) was added to paragraph 2, as the page title used "lean budget" but the intro never defined it operationally; the CRM vs PM comparison routing signal was added to paragraph 3, which previously only routed to the workflow anchor and overbuying guide but not the system-center comparison; internal SEO language removed from paragraph 4 ("this is the strongest next indexing candidate for the stack cluster" — AGENT.md prohibits internal planning language in public-facing copy); the heading "If you landed here directly" renamed to "When this page is the right starting point" matching the improvement pattern from the workflow anchor pass; Hugo build clean at 75 pages with 87 static files; QA confirmed all key changes present and internal SEO language absent in `public/guides/software-stack-blueprint-solo-freelancer-lean-budget/index.html`; `public/` remains ignored and untracked; chosen as today's daily pass because the lean blueprint intro had the clearest set of fixable weaknesses — buried who, undefined title term, missing comparison routing, and prohibited language — all addressable surgically without touching the blueprint body.

- 2026-04-21 workflow anchor intro tightening: tightened the opening 4 paragraphs of `content/client-workflow-systems/sample-client-workflow.md` into 3 sharper paragraphs — the "use this before changing tools" signal moved from paragraph 4 to paragraph 3 where it now reads as an early, clear directive rather than a buried afterthought; the old paragraph 3 ("Then implement each stage using the linked guide...") was absorbed and simplified since the concept is already covered by later sections; the heading "If you landed here directly" was renamed to "When this page is the right starting point" to read as useful guidance rather than a conditional statement; a skip-ahead anchor link to `#how-to-choose-the-first-stage-to-fix` was added at the end of that section, giving readers who are already close to a diagnosis a direct fast-path without restructuring the page; Hugo build clean at 75 pages with 87 static files; QA confirmed all three changes rendered correctly in `public/workflows/freelance-client-workflow-system-inquiry-to-final-payment/index.html`; `public/` remains ignored and untracked; chosen as today's daily pass because the anchor page intro was the strongest remaining first-screen improvement: the tool-change signal was the most important missing-from-top message and the routing section lacked a fast-path exit for readers who did not need the full preamble.

- 2026-04-20 topic-specific feature image pass: added 3 new Python templates (`checklist_card`, `concept_map`, `faq_card`) and 29 JSON data configs; generated 26 new WebP images across 5 groups (blueprint hub + 3 cards, comparisons hub + 5 cards, templates hub + 4 cards, glossary hub + 5 cards, FAQ hub + 4 cards); updated front matter in 30 content pages to reference `.webp` instead of `.svg` — also updated `client-workflow-systems/_index.md` and `sample-client-workflow.md` which already had approved WebPs but still referenced `.svg`; the 4 original approved WebPs (workflow anchor, workflow hub, lean blueprint, CRM vs PM comparison) were not touched (timestamps unchanged); Hugo build clean at 75 pages with 87 static files (30 WebPs total); no broken references; `public/` remains ignored and untracked. **Templates added:** `checklist_card` (trigger + 4-step rows), `concept_map` (central concept box + 2x2 attribute grid), `faq_card` (problem box + resolution routes). **Data dirs added:** `data/templates/`, `data/glossary/`, `data/faq/`. Codex weekly review should verify rendered card images look visually distinct at card size across all 5 groups.



- 2026-04-20 client-workflow-systems feature image connector fix: the connector logic in `tools/feature-images/feature_images/templates/hub_overview.py` was drawing horizontal lines from the left-gap trunk (x=676) all the way to x=1090, crossing straight through the Intake and Billing module boxes and creating a shaded-block appearance; fixed by replacing those four-target loop lines with two separate sets: (1) short left-gap lines from trunk to left-column module edges (x=676→708), and (2) short right-gap lines between left-column right edges and right-column left edges (x=1048→1090); no lines now cross through any boxes; the connector flow is anchor → Intake + Billing (left gap) → Delivery + Closeout (right gap); regenerated `static/images/features/hubs/client-workflow-systems.webp` via `generate_one.py --id client-workflow-systems`; Hugo build clean at 75 pages; confirmed image referenced 3 times in build output, all other 3 approved WebPs untouched (timestamps unchanged from 2026-04-17); the four approved WebPs remain exactly the approved set.
- 2026-04-20 workflow anchor 7-day plan links: added five internal links to the "First 7-day implementation plan" section of `content/client-workflow-systems/sample-client-workflow.md`; Day 2 now links to the intake and qualification workflow, Day 3 links to the proposal-to-contract handoff guide, Day 4 links to the onboarding checklist template, Day 5 links to the invoice and payment workflow setup, and Day 6 links to the client offboarding workflow; this section is an implementation action list where readers act day-by-day, making it a natural location for direct page links; Hugo build clean at 75 pages; QA confirmed all five new links rendered in `public/workflows/freelance-client-workflow-system-inquiry-to-final-payment/index.html`; chosen as today's daily pass because the 7-day plan was the last significant unlinked action section on the anchor page after the previous pass covered the stage-choice section; no Codex review flag needed beyond what was already noted for the prior anchor pass.
- 2026-04-20 workflow anchor internal links: added six stage-level internal links to the "How to choose the first stage to fix" section of `content/client-workflow-systems/sample-client-workflow.md` (the workflow anchor); each bullet in that section now ends with "Use the [stage name link]" pointing to the corresponding stage workflow page (intake, proposal handoff, onboarding, milestone delivery, invoice/payment, offboarding); this section is a decision-point moment where a reader is choosing their next step, making it the clearest location for direct stage links; existing links elsewhere on the page were not duplicated or changed; Hugo build clean at 75 pages; QA confirmed all six new link targets rendered correctly in `public/workflows/freelance-client-workflow-system-inquiry-to-final-payment/index.html`; chosen as today's daily pass because the anchor page is the highest-priority page on the site and the "choose first stage" section had no outbound links despite being the primary routing decision moment; Codex weekly review should verify the link anchor text and surrounding sentences read naturally in context.
- 2026-04-20 hub page redundancy cleanup: removed three redundant H2 sections from `content/client-workflow-systems/_index.md` — "The safest first path through this hub", "Use this hub as a sequence, not a library", and "Fastest useful starting points" — all of which duplicated content already present in "How to use this hub" and the more complete "Start with the broken stage" numbered list; page structure is now tighter with 11 sections instead of 14; Hugo build clean at 75 pages; QA confirmed removed sections absent and retained sections present in rendered output; chosen as the daily pass because TRACKER recommended a first-screen/quality refinement on the workflow hub if it still lags, and eliminating obvious section redundancy is the clearest quality signal improvement without changing any facts or links; Codex weekly review may optionally verify the page still flows naturally after the structural trim.
- 2026-04-18 site name signals: added `og:site_name` (was absent) and `alternateName: "Solo Ops Guide"` to the `WebSite` JSON-LD in `layouts/partials/head.html`; homepage `WebSite` schema already had `name: "SoloOpsGuide"` and homepage title already led with the brand name — no conflicts; Hugo build clean; verified `og:site_name`, `WebSite` JSON-LD with both name and alternateName, and homepage title in `public/index.html`; commit `Strengthen homepage site name signals` pushed successfully.
- 2026-04-17 operating rhythm docs: added the Claude Code daily rhythm and Codex weekly/monthly review rhythm to `AGENT.md` and recorded it in the live workflow summary here; verified only `AGENT.md` and `TRACKER.md` changed; no Hugo build required; commit `99e701d` (`Document Claude and Codex operating rhythm`) pushed successfully.
- 2026-04-17 local cache ignore cleanup: updated `.gitignore` to ignore Python cache files and `.claude/settings.local.json`; removed generated `__pycache__` directories from the worktree; no Hugo build required for this cleanup.
- 2026-04-17 tracker cleanup: reorganized `TRACKER.md` into a live working tracker and moved older detailed history into [project-docs/TRACKER-ARCHIVE.md](project-docs/TRACKER-ARCHIVE.md); docs-only change, no Hugo build required.
- 2026-04-17 slug and BOM stability: added explicit slugs to the workflow anchor, intake guide, lean blueprint, and CRM-vs-PM comparison while preserving current public URLs; removed the UTF-8 BOM from the workflow anchor source (`f254c19`).
- 2026-04-17 image cleanup: removed 20 unapproved orphan WebPs, leaving exactly 4 approved WebPs plus SVG fallbacks (`f3e935d`).
- 2026-04-17 deployment policy cleanup: clarified Cloudflare Pages source-build deployment and removed `public/` from tracked source (`74adfc7`).
- 2026-04-17 approved image refresh: regenerated and then refined the 4 approved WebPs through the Python/Pillow workflow (`f9fffa1`, `4a28c41`).
- 2026-04-16 repo-guidance alignment: merged durable external guidance into `AGENT.md` without replacing repo-specific SoloOpsGuide rules (`95eb0c0`).
- 2026-04-13 to 2026-04-15 foundation work: tightened homepage and hub routing, trust pages, crawl signals, and the official feature-image workflows. See the archive for the pass-by-pass log.

## Deferred issues / backlog

- GSC wait/watch remains open: do not assume the indexing problem is solved until the workflow hub and related first-index pages move in Search Console.
- Duplicate page classification logic review is still deferred.
- Remaining lower-priority sample-slug cleanup is still deferred for:
  - `content/workflow-comparisons/sample-notion-vs-clickup.md`
  - `content/templates-checklists/sample-onboarding-checklist.md`
  - `content/glossary/sample-workflow-definition.md`
- PowerShell image-generator readability and cleanup remains deferred; the current approved raster path is the Python/Pillow workflow, so PowerShell cleanup is lower priority unless that path is revisited.
- Templates/checklists remain the easiest support layer to misuse if readers skip upstream workflow pages.
- A standalone `/methodology/` page is still optional, not required; current editorial-method coverage remains acceptable through `Editorial Policy` and `Content Policy`.

## Next recommended actions

- Wait for more GSC movement before making broader SEO or architecture changes.
- If the workflow hub still lags, do one very small first-screen refinement on `/client-workflow-systems/` only.
- If support pages continue to outrank cornerstone paths, audit homepage-to-workflow routing and a few already indexed support pages for stronger contextual links back into the workflow cluster.
- If feature-image work resumes, keep the raster gate at the current four approved pages unless a new output set passes visual review.
- If a later cleanup pass is needed, the safest next docs/content-adjacent item is the remaining lower-priority sample-slug sweep.

## Historical archive / compressed log

- Detailed history now lives in [project-docs/TRACKER-ARCHIVE.md](project-docs/TRACKER-ARCHIVE.md).
- Use the archive when you need older verification notes, detailed image-workflow evolution, or commit-level historical context from the 2026-04-13 to 2026-04-17 passes.
