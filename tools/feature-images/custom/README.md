# Custom Generators

Leave this directory empty unless a future feature image truly cannot fit a shared layout template cleanly.

Custom generators should stay rare and should still reuse the shared helpers in `tools/feature-images/feature_images/`.
